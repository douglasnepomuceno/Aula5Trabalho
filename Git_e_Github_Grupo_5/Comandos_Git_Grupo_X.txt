

#Cria o diretorio DH
mkdir DH

#Entra no diretorio DH 
cd DH

#Cria o diretorio
mkdir integracao_aula_1e2

cd integracao_aula_1e2

#Cria o diretorio
mkdir historia_da_contabilidade
#Cria o diretorio
mkdir o_que_faremos_hoje
#Cria o diretorio
mkdir servidores
#Cria o diretorio
mkdir interfaces_de_usuario
#Cria o diretorio
mkdir infraestrutura

#Entra no diretorio
cd historia_da_contabilidade

#Cria o arquivo 
touch resumo.txt
#Retorna um nivel do diretorio
cd ..
#Entra no diretorio
cd o_que_faremos_hoje

#Cria o arquivo 
touch resumo.txt

#Retorna um nivel do diretorio
cd ..
#Entra no diretorio
cd servidores

#Cria o arquivo 
touch resumo.txt

#Retorna um nivel do diretorio
cd ..
#Entra no diretorio
cd interfaces_de_usuario

#Cria o arquivo 
touch resumo.txt

#Retorna um nivel do diretorio
cd ..
#Entra no diretorio
cd infraestrutura

#Cria o arquivo 
touch resumo.txt


git init
Este comando é usado para criar um novo repositório GIT. Local

git add
O comando git add pode ser usado para adicionar arquivos ao índice do git local 

git clone
O comando git clone clona um repositorio para maquina local 

git commit
O comando git commit é usado para confirmar as alterações feitas nos arquivos


git status
O comando git status exibe a lista de arquivos alterados juntamente com os arquivos que ainda não foram adicionados a camada de stage

git status

git push Envia as alterações feitas para o ramo/brach repositório remoto associado ao diretório de trabalho

Exemplo
git push origin master

git checkout
O comando git checkout pode ser usado para criar ramos/branch 

pdw: Mostra o repositório no qual estou localizado;
ls: Lista os arquivos e repositórios atuais;
cd: Entra no diretório especificado;
mkdir: Cria um repositório
touch: Cria um arquivo;
vi: Abre o editor de texto do GitBash;
exit: Fecha o GitBash;
git --version: Mostra a versão atual do GitBash;
git status: Mostra o status do commit atual;
git add: Adiciona os arquivos ao commt;
git commit -m "meu primeiro commit": Commita os arquivos;
clear: Limpa a tela do GitBash;
git init: Inicia o repositório;
rm -r: Exlui um arquivo;
git config user.name: Configura seu user name no Git;
git config user.email: Configura seu e-mail no Git;
git remote add origin <link>: Adiciona um repositório remoto;
git push origin main: Envia os arquivos para o repositório remoto;
git clone <link>: Clona um repositório remoto;
git branch: Mostra as branchs disponíveis;
git checkout <branch name>: Entra na branch especificada;
git pull: Baixa os arquivos do repositório remoto;
code: Abre o código especificado.
